#include "main.h"

/**
 * strlen - returns the length of a string.
 *
 * @s: input string.
 *
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int index;

	For (index = 0; s[count] != '\0' ; index++)
		;
	return (index);
}
