#include <stdio.h>

/**
 *main -> assign a random number to the variable
 *
 *Return : always 0
 */
int main(void)
{
	for (ch = 'z'; ch >= 'a'; ch++)
	{
		
		putchar(ch);
	}
	putchar(10);

	return (0);
}
